/**
 * TallCMS Core Runtime
 *
 * Shared Alpine.js components and plugins required by native CMS blocks.
 * This is built once from the root Vite config and loaded globally via
 * @tallcmsCoreJs, so themes do NOT need to bundle it themselves.
 */

import './tallcms';
